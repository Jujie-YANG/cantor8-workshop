# Optional. Copy to .env only if you want to override defaults.
# Do not commit .env.

IDP_BASE=https://auth.dev.digik.cantor8.tech
VALIDATOR_API_BASE=https://api.validator.dev.digik.cantor8.tech/api/validator

# Optional auth fallback (candidate_3). The script tries workshop credentials automatically.
CLIENT_SECRET=

# Party hint for topology generate (default: jacky)
PARTY_HINT=jacky

# Optional, for check_balance.py / check_acs.py after main.py succeeds
PARTY_ID=

HTTP_TIMEOUT_SECONDS=30
